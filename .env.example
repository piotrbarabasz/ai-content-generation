# Placeholder-only local environment file.
# Copy to .env for local use. Never commit real secrets.

APP_ENV=local
ARTIFACT_ROOT=./data/artifacts

LLM_PROVIDER=mock
LLM_API_KEY=placeholder-only
TTS_PROVIDER=mock
TTS_API_KEY=placeholder-only
CAPTION_PROVIDER=mock
CAPTION_API_KEY=placeholder-only
VIDEO_RENDERER_PROVIDER=mock
STORAGE_PROVIDER=local
