#!/bin/sh
set -eu

PYTHON_BIN="$(git config --local --get agent.python || true)"
if [ -z "${PYTHON_BIN:-}" ]; then
  PYTHON_BIN=python
fi

exec "$PYTHON_BIN" -m backend.app.tooling.git_hook_runner pre-push
